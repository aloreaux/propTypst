import { $ } from "execa";
import getTypstPath from "./lib/getTypstPath.js";
export default async function help(options = {}) {
    const { stdout } = await $({
        signal: options.signal,
    }) `${getTypstPath()} --help`;
    return stdout;
}
