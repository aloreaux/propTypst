/**
 * "Absorbs" the subprocess into the current process. Sort of like
 * how `exec othercmd "$@"` works in Bash. Synchronous. Always
 * triggers `process.exit()`.
 */
export default function abexec(argv0: string, argv: string[]): void;
